# CSAR Development Environment Variables
# Copy to .env and adjust as needed

# KMS local keys (keyID=passphrase format)
CSAR_KMS_LOCAL_KEYS=dev-key=dev-passphrase

# Token file path (local dev only)
CSAR_TOKEN_FILE=tokens.yaml

# Metrics
CSAR_METRICS_ADDR=:9100
